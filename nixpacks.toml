[phases.setup]
nixPkgs = [
  "nodejs_18",
  "python312",
  "python312Packages.numpy",
  "python312Packages.pandas",
  "python312Packages.matplotlib",
  "python312Packages.scipy",
  "python312Packages.pip"
]

[phases.install]
cmds = ["npm ci", "python3 -m pip install --quiet --break-system-packages yfinance"]

[start]
cmd = "node server.js"
