[phases.setup]
nixPkgs = ["nodejs_18", "python312", "python312Packages.pip"]

[phases.install]
cmds = ["npm ci", "pip3 install --quiet --break-system-packages yfinance pandas numpy matplotlib scipy"]

[start]
cmd = "node server.js"
