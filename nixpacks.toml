[phases.setup]
nixPkgs = ["nodejs_18", "python3", "python3Packages.pip"]

[phases.install]
cmds = ["npm ci", "pip3 install --break-system-packages yfinance pandas numpy matplotlib scipy"]

[start]
cmd = "node server.js"
