[phases.setup]
nixPkgs = ["nodejs_18", "python312"]

[phases.install]
cmds = [
  "npm ci",
  "python3 -m venv /app/pyenv",
  "/app/pyenv/bin/pip install --quiet numpy pandas matplotlib scipy yfinance"
]

[start]
cmd = "node server.js"
